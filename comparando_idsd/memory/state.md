# Estado Operacional — IDSD
_Atualizado em: 2026-09-16_

> Estado atual do sistema e das demandas em andamento.
> Para princípios estáveis e Decision Records, veja [constitution.md](constitution.md).
> Para o registro integral de gates e o histórico narrativo, veja
> [historico.md](historico.md) — arquivo de consulta, não carregado por padrão.

---

## Sistema

| Campo | Valor |
| --- | --- |
| Nome | IDSD |
| Cenário | Novo (greenfield) |
| Guidelines | vinculadas — `guidelines.yaml` → `requirements/guidelines` (`_shared`, `backend/java`, `frontend/nextjs`, `infra/docker`) |
| Inicializado em | 2026-09-03 |

Os flows disponíveis não são listados aqui: eles vivem em `governance/flows/`,
e essa é a única fonte. Copiar a cadeia de etapas para cá cria uma segunda
versão que envelhece em silêncio — foi o que aconteceu na stack anterior.

Regenerar os derivados: `python .agents/scripts/init.py`.
Verificar sincronia: `python .agents/scripts/check_drift.py`.

---

## Demandas ativas

| Demanda | Flow | Classe | Etapa atual | Estado |
| --- | --- | --- | --- | --- |
| kanban-tarefas | `feature` | feature / risco alto / impacto sistema | **Fechamento da revisão do board em curso: o lado da spec está pronto — PRD v1.6, TechSpec v1.18, SDR-007.** ACH-03 fechado por RN-039/RNF-011/SCN-003.4; ACH-01, ACH-02, ACH-05 e ACH-08 decididos e escritos no contrato. Faltam `/implement` — os quatro acima mais ACH-06, 07, 09 e os oito menores — e `/tests`, que é ACH-04 | Cadeia liberada: PRD v1.5, TechSpec v1.17, plano de verificação v1.9, 8 épicos / 43 tasks, 70/70 cenários congelados cobertos. Os 17 achados da **primeira** revisão de TASK-02.2 estão fechados e a serialização de SDR-005 entrou. A **reexecução** abriu **23 achados novos, 4 bloqueantes** — 15 do `/tests`, 6 do `/implement`, 2 do `/techspec`. **Os 4 bloqueantes estão fechados** — ACH-01 pelo teto de espera de SDR-005 (TechSpec v1.13) e ACH-02/03/04 pelo `/tests` (plano v1.7), os três verificados pela assimetria. Os **19 restantes também estão fechados** — `/implement` 6, `/techspec` 2, `/tests` 11 —, e o fechamento acrescentou 18 testes, quatro deles com poder de falha provado por mutação. **0 achados em aberto.** A tese da revisão: o conserto dos bloqueantes anteriores entrou sem verificação, e a única verificação escrita nesta mão, `SubstituicaoDeFluxoConcorrenteIT`, **não ficava vermelha se o bloqueio de SDR-005 fosse removido** — agora fica. Ver `docs/review/kanban-tarefas/TASK-02.2-review-r2.md`. **TASK-02.3 implementada, medida 10/10 e revisada (2026-09-15): 8 achados, 1 bloqueante em aberto** — ACH-01, RNF-008 garantido de um lado só, que não é defeito da implementação e sim a pendência 21. **Os outros 7 foram fechados no mesmo dia** — `/techspec` (TechSpec v1.15: bloqueio otimista em `impedimento`, regra de FK dentro do anel de projeção, índice da fila), `/tasks` (cenário coberto retirado, tabela de arquivos completada) e `/implement` (migration de ordem 7, `@Version`, três textos corrigidos), com 189 / 96 / 93 medido depois e zero regressão. ACH-08 rebaixado a menor com aprovador humano nomeado. **O bloqueante ACH-01 saiu por TASK-02.9**, task de infraestrutura nova, decidida pelo demandante: a role de login `kanban_app` passa a ser a credencial da aplicação e o arnês limpa o banco com a credencial do dono — fecha as pendências 21 e 22 no mesmo passo e torna verdadeira a promessa de `contracts/sessao-e-projetos.md:118`, que por isso não precisa de emenda. Ver `docs/review/kanban-tarefas/TASK-02.3-review.md`. **TASK-02.5 revisada em 2026-09-15: 14 achados, 4 bloqueantes** — três de código na borda nova (`raiaId` sem escopo de projeto, FK sem tradutor, leitura do fluxo antes do bloqueio) e um de dependência de task (cinco dos dez critérios sem medição, dono `/tasks`). Ver `docs/review/kanban-tarefas/TASK-02.5-review.md`. |

**Escopo congelado.** 70 cenários — 9 `e2e`, 56 `integração`, 5 `unitário` —,
mais 14 verificações além dos cenários e 4 desvios declarados.

**Progresso.** EPIC-01 fechado (TASK-01.1 a 01.8; a ordem executada não foi a
numérica, porque 01.7 depende de 01.8). EPIC-02 aberto com TASK-02.1 concluída
e revisada, e TASK-02.2 concluída e revisada **duas vezes** — a segunda abriu
4 bloqueantes, todos fechados e verificados no mesmo dia. TASK-02.3 concluída,
revisada e com os 8 achados fechados, e TASK-02.9 — task de infraestrutura
nascida do bloqueante dela — concluída e medida. **TASK-02.4 implementada**
— o núcleo de escrita —, revisada, com os 13 achados de código fechados e a
suíte medida por inteiro sem regressão. **Os sete critérios de aceite dela
seguem sem medição, e não por ambiente:** as três classes que os exercitariam
param em `404` da rota de criação de tarefa, que nasce em TASK-02.5. ACH-05
mudou de destino — de `/implement` para `/tasks`. **TASK-02.6 implementada e
medida**, e ela dissolve **só metade** do que se esperava: o board existe e
acende 7 testes, mas ACH-04 de TASK-02.5 e ACH-05 de TASK-02.4 **continuam
abertos**, porque a maior parte dos critérios que eles cobram passa por
`GET /v1/tarefas/{tarefaId}` — rota que, descoberto aqui, **não era declarada por
task nenhuma**. **Resolvido pelo `/tasks` em 2026-09-16:** a rota é **TASK-02.10**,
e os dois achados passam de dívida sem endereço a dependência de task nomeada —
a medição deles é pré-condição de fechamento daquela task, não das duas que os
registraram.

**Suíte.** **198 testes, 117 verdes / 81 vermelhos** (2026-09-15, após TASK-02.6),
contra 198 / 110 / 88: os 7 a mais são o board acendendo — dois de `BoardIT`, um de
`RaiasIT`, três de `CriacaoDeTarefaIT` e um de `ConfiguracaoDoFluxoIT` —, zero
regressão. A medição anterior era **198 testes, 110 verdes / 88 vermelhos** (após TASK-02.5),
contra 190 / 96 / 94: os 8 a mais são `CriacaoDeTarefaServiceTest`, que passou a
compilar e saiu 8/8 verde, e outros 6 vermelhos acenderam — zero regressão. Os
arquivos de teste que não compilam caem de 3 para **2**. A medição anterior era
**190 testes, 96 verdes / 94 vermelhos** (após TASK-02.4),
contra 189 / 96 / 93: o teste a mais é o arquivo a mais que passou a compilar, e
sai vermelho pela mesma dependência das demais — zero regressão. A medição
anterior era **189 testes, 96 verdes / 93 vermelhos**, contra a base
de 189 / 91 / 98: TASK-02.3 acendeu cinco e não apagou nenhum — zero regressão. Os vermelhos são a linha de
base — cenários de tasks ainda não implementadas —, e a suíte **não compila** na
árvore de trabalho, porque os cenários unitários nomeiam classes de tasks
posteriores; a medição exige excluí-los em cópia descartável. São 4 os arquivos
que restam nessa condição — **3 desde TASK-02.4**, que criou `RegistradorDeEvento`
e `AplicadorDeIntervalos`; antes eram 4, e antes de TASK-02.2 criar
`EtapaService`, `FluxoRequisicao` e `RegraDeNegocioViolada`, 5. Que os três
restantes passem a resolver essas duas classes é a evidência direta de que o
contrato da task bate com a suíte congelada.

**4 dos 5 testes de `ConfiguracaoDoFluxoIT` seguem vermelhos por dependência e
não por defeito:** eles exercitam o board e a criação de tarefa, que nascem em
TASK-02.6 e TASK-02.5. O fluxo de etapas só fica verificável de ponta a ponta
quando o épico fechar — é a mesma razão que mantém GATE-NFR reprovado.

**Q-012 distribuída em 2026-09-16** — vira **TASK-01.9**, dívida declarada fora
do gate de EPIC-01, com prazo por evento externo (o primeiro realm federado) e
não por posição na fila. Era a última questão da TechSpec com dono `/tasks` e
sem task.

**A rota órfã tem dono: TASK-02.10.** `GET /v1/tarefas/{tarefaId}` é declarada
pelo contrato desde a v1.0 e exercitada por **onze classes** da suíte congelada,
e não pertencia a task nenhuma. Ela é pré-condição da medição dos critérios em
aberto de TASK-02.4 (ACH-05) e TASK-02.5 (ACH-04) — os dois achados deixam de ser
dívida sem endereço e passam a ser dependência de task nomeada.

---

## Gates atravessados

> Uma linha por gate, com o veredito final. O detalhe de cada achado está em
> [historico.md](historico.md); os relatórios de revisão, em `docs/review/`.

| Demanda | Gate | Data | Veredito |
| --- | --- | --- | --- |
| kanban-tarefas | GATE-INTENT | 2026-09-04 | aprovado — Thiago Goncalves Cavalcante (demandante) |
| kanban-tarefas | GATE-CLASSIFY | 2026-09-04 | aprovado — agente, sem override |
| kanban-tarefas | GATE-CONTEXT | 2026-09-04 | aprovado — 0 lacunas bloqueantes |
| kanban-tarefas | GATE-DIRECAO | 2026-09-04 | aprovado — Thiago Cavalcante, sem ressalvas |
| kanban-tarefas | GATE-SPEC | 2026-09-09 | aprovado — 4 reconfirmações de emenda até a v1.5 (2026-09-10); H-03 aberta por decisão da etapa |
| kanban-tarefas | GATE-PROVENIENCIA | 2026-09-09 | aprovado — 1 inferência (RNF-009) confirmada pelo demandante |
| kanban-tarefas | GATE-CONSISTENCIA | 2026-09-10 | **aprovado** na 9ª execução (`--pre-implement`) — 0 achados, 0 waiver. As 8 anteriores reprovaram; os 22 achados estão fechados |
| kanban-tarefas | GATE-RASTREABILIDADE | 2026-09-09 | aprovado — invariante cenário↔épico fechada nas duas direções |
| kanban-tarefas | GATE-VERIFICACAO-INDEPENDENTE | 2026-09-10 | aprovado — 0 arquivos de produção lidos ou escritos. **Reavaliar:** o plano v1.6 acrescentou `SubstituicaoDeFluxoConcorrenteIT` em modo `audit`, com 3 arquivos de produção lidos. A exceção está declarada no artefato; o gate precisa ser julgado sabendo dela |
| kanban-tarefas | GATE-GHERKIN-CONGELADO | 2026-09-10 | aprovado — 70/70 cobertos, 0 cenários alterados desde o gate de spec |
| kanban-tarefas | GATE-REVISAO-TECNICA | parcial | **reprovado apenas por não haver épico fechado** — nenhum achado em aberto. 14 revisões parciais (TASK-01.1 a 01.8, 02.1, 02.2 duas vezes, 02.3, 02.4, 02.5, 02.6). As 12 primeiras estão com todos os achados fechados; a de TASK-02.6 abriu **17, 4 bloqueantes, todos em aberto** — ACH-01 (campo do board exigido pela suíte congelada e ausente do corpo), ACH-02 (cartão em etapa arquivada some sem erro), ACH-03 (board sem envelope de desempenho e sem recorte) e ACH-04 (cinco critérios sem verificação que fique); a de TASK-02.5 abriu 14 e **os 3 bloqueantes de código fecharam no mesmo dia, medidos** (escopo de projeto da raia, tradução da FK e bloqueio antes da leitura do fluxo); resta **1 bloqueante em aberto, ACH-04**, que é dependência de task e não cabe no escopo de arquivo revisado. Os 8 de TASK-02.3 fecharam em 2026-09-15: sete dentro do épico e o bloqueante ACH-01 por TASK-02.9, que é onde ele sempre coube — o escopo de arquivo da task revisada não o alcançava |
| kanban-tarefas | GATE-NFR | parcial | **reprovado** — os envelopes só são mensuráveis no fechamento de épico. RNF-004, RNF-008 e RNF-010 medidos dentro. A reexecução de TASK-02.2 acrescentou uma segunda razão: SDR-005 introduziu espera por lock numa rota autenticada sem envelope de espera. **O envelope passou a existir em 2026-09-14** — 5 s de espera pelo bloqueio, 10 s de transação, 5 s por conexão —, mas está **declarado e não medido**, como os outros sete. **2026-09-15:** o primeiro envelope efetivamente medido saiu **fora** — RNF-008 — e **entrou no mesmo dia**, por TASK-02.9: a perna de banco deixou de ser inerte quando a aplicação passou a conectar por `kanban_app`, que não é superusuária nem dona do schema. Nenhum envelope medido está fora; o gate segue reprovado só pelos sete declarados e não medidos e pelo épico aberto. **2026-09-15, TASK-02.4:** a suíte foi medida e RNF-002 e a perna de aplicação de RNF-008 **continuam sem medição** — não por ambiente, e sim porque as classes que os exercitam param na rota de TASK-02.5. O gate só se torna decidível quando o épico fechar, o que é a mesma razão de sempre, agora com evidência em vez de suposição |

---

## Devoluções

> Contadores do anti-ping-pong. Ver o orçamento vigente na policy.

| Demanda | Por par | Por diamante | Total |
| --- | --- | --- | --- |

---

## Pendências conhecidas

| # | Pendência | Efeito |
| --- | --- | --- |
| 01 | Ambiente integrado efêmero para o gate E2E de épico multi-sistema. | Enquanto não existir, esse gate é aprovação manual. |
| 03 | `frontend/react` é stub na biblioteca; não governa este sistema. | Nenhum, enquanto nenhum sistema declarar a coleção. |
| 04 | Não existe mapa de domínios do sistema, embora o template de classificação o pressuponha. | O eixo Domínio de `kanban-tarefas` é provisório; reconciliar quando o mapa existir. |
| 05 | `.agents/skills/design/SKILL.md` (Fase 3) prescreve seções diferentes das que `validate-rules.json` exige. | Todo Design Brief escrito conforme a skill reprova no validador. Corrigir a fonte — provavelmente a lista da SKILL.md. |
| 06 | `check_proveniencia.py` reprova dúvida cujo Status não **comece** literalmente com `resolv`/`fechad`; negrito quebra. | Marcação em negrito bloqueia o GATE-SPEC sem que a causa apareça na mensagem. |
| 11 | `check_task_files` exige ao menos um item `- [ ]`, de modo que **task concluída sempre reprova**. | O validador do plano de tasks nunca mais sai em exit 0, e o sinal deixa de distinguir defeito de progresso. |
| 20 | `check_veredicto.py` lê a tabela de fechamento do relatório de revisão como se fosse a tabela de Achados: cada linha de fechamento vira quatro erros (severidade, local, descrição, destino). Descoberto em 2026-09-14, ao medir a pendência 18. Dono: ferramenta. | O validador do `/code-review` sai com dezenas de erros que não correspondem a defeito, e o sinal real fica submerso. |
| 16 | Em `docker/compose.yaml`, `PORTA_IDENTIDADE` e `IDENTIDADE_HOSTNAME` são independentes: porta deslocada sem hostname deslocado manda o navegador para outra stack da máquina. Dono `/tasks`/infra. | O sintoma aparece a um serviço de distância da causa. Custou uma sessão de diagnóstico em 2026-09-11. |
| 24 | `check_task_files.py` exige ao menos um `SCN-NNN.N` no campo **Cenários cobertos** de toda task, mas a invariante do gate é **cenário↔épico** e não cenário↔task. Task de infraestrutura (TASK-02.9), de superfície compartilhada (TASK-02.10) e de correção de borda (TASK-01.9) não entregam cenário próprio, e declará-lo quebraria a invariante nos dois sentidos. Descoberta em 2026-09-16; precedente desde TASK-02.9. Dono: ferramenta. | Três erros por execução que não correspondem a defeito, somados aos de pendência 11 — o validador do plano de tasks já não distingue defeito de progresso. |
| 17 | O validador da TechSpec lê menção a task como planejamento de task. É rastreabilidade, não plano. Dono: ferramenta. O número de linha migra a cada emenda — em 2026-09-15 é `kanban-tarefas-techspec.md:244`, a menção a `TASK-02.2`; **não fixe a linha ao conferir**, e confirme que o erro é este antes de tratá-lo como conhecido. | Toda revisão da TechSpec sai com um erro que não corresponde a defeito. |

**Resolvidas** (registro em [historico.md](historico.md)): 21 role de aplicação distinta do dono, 22 contradição da suíte quanto ao `TRUNCATE` e 23 decisão sobre ACH-01 — as três fechadas em 2026-09-15 por TASK-02.9, e as três pelo mesmo movimento: separar quem prepara o ambiente de quem exerce o produto · 02 coleção
`infra/docker` elaborada · 07 `check_gherkin` em bloco cercado · 08 propagação
na TechSpec · 09 rota de criação de projeto · 10 `{{FEATURE}}` com sufixo
`-verificacao` · 13 `---` lido como patch no `/code-review` · 14 SCN-002.4 sem
teste, fechada em 2026-09-14 pelo `/tests` (plano v1.6, cobertura 70/70) · 15
RNF-005 sem dono · 12 prefixo de caminho em `check_escopo.py`, resolvida em
2026-09-14 por `git rev-parse --show-prefix` · 18 erro rebaixado a aviso em
`run_custom_steps`, resolvida em 2026-09-14 — a acusação original (exit 0 com
`ERRO:`) não se reproduziu nos scripts, mas o caminho que a produz existia no
`validate.py` · 19 drift de `.github/instructions/`, resolvida em 2026-09-14:
era fim de linha e não edição à mão, e `generate_platform.py` passou a gravar
em LF.

---

## Histórico

> Registro integral, com as razões de cada decisão, em
> [historico.md](historico.md). Abaixo, só a linha do tempo.

| Data | Mudança |
| --- | --- |
| 2026-09-16 | `/code-review TASK-02.6` — **17 achados, 4 bloqueantes**, e o primeiro deles é a suíte congelada não atendida sem que nenhuma medição pudesse acusar. **ACH-01:** `AdminGlobalIT` afirma `$.acessoPorAdministracaoGlobal` **sobre a resposta do board**, na mesma requisição em que confere as 4 etapas, e `BoardResposta` tem só `(seq, etapas)`. A divergência é de três pontas — a suíte exige, o contrato do board omite (enquanto `sessao-e-projetos.md` o declara na lista e no detalhe), a implementação não emite —, e ela é **invisível à medição**: aquela classe já morre adiante, em rota de EPIC-03, de modo que o defeito só apareceria quando o épico seguinte fechasse, atribuído à task errada. **ACH-02 é a assimetria entre os dois eixos da grade:** a frase que justifica a raia sintética — cartão que não cabe na grade some, e some sem erro — vale inteira para a **etapa arquivada**, e ali não há defesa nenhuma; as tarefas são lidas sem filtro de etapa e a célula da etapa arquivada nunca é montada. **Não é hipótese:** `ContagemDeTarefasAtivas` exclui as condições terminais da contagem que barra o arquivamento, **por decisão declarada**, justamente para que a etapa terminal seja arquivável — logo uma operação permitida apaga da única tela todo o trabalho concluído ali. **ACH-03 é de envelope e a descoberta é que ele não existe:** a task declara RNF-009 na origem, e o PRD atrela RNF-009 a RF-015/016 e não a RF-003 — **o board, a leitura mais exercitada do produto e a que todo cliente refaz a cada reconexão, não tem envelope de tempo de resposta em RNF nenhum**; e a consulta traz toda tarefa já criada no projeto, sem recorte, sem janela e sem paginação, de modo que o número de consultas é fixo mas o payload cresce sem teto com a idade do projeto. **ACH-04:** cinco dos sete critérios sem verificação **que fique** — 5 e 7 foram medidos por sonda descartável, e trocar a montagem em memória por consulta por cartão deixa a suíte inteira verde; 6 está coberto pela metade, porque projeto **inexistente** não tem teste e é justamente o caminho que depende da linha de existência do controlador. Entre os relevantes, `permanencia` declarada não-nula no contrato e emitida nula no caso mais comum do board, o NPE cuja guarda mora em outra classe enquanto o Javadoc institui que a consulta "não decide acesso" (e `montar` é o modelo declarado de TASK-02.10), o bloco de impedimento e o seu motivo vindos de **fontes diferentes** — divergência sai como `motivo: null` silencioso num campo `not null` — e `assumidaEm` ausente do cartão, que esta task fixa e TASK-02.7 herdará. **Nenhum achado de segurança:** as cinco consultas filtram por projeto, não há vazamento cross-projeto — o oposto do que TASK-02.5 trouxe —, JPQL parametrizado em toda parte, `404` antes de `403` e `detail` genérico. **A suíte não foi reexecutada:** o Docker Desktop está parado, e o veredicto de regressão segue sendo o que o `/implement` declarou. Os 3 erros de `check_veredicto.py` são sinal correto. Ver `docs/review/kanban-tarefas/TASK-02.6-review.md` |
| 2026-09-16 | **Os quatro bloqueantes da revisão do board fechados do lado da spec — PRD v1.6 e TechSpec v1.18.** Duas mãos, spec antes de código. **`/prd`:** nasce **RN-039** — o board exibe tarefa terminal só enquanto o desfecho for dos últimos 30 dias, e as mais antigas saem da tela e continuam íntegras na ficha e nas consultas de andamento e tempo por etapa, porque **o recorte é da tela e nunca do registro** (RN-022, RNF-008) — e nasce **RNF-011**, p95 ≤ 2 s com 5.000 tarefas, **mesmo volume de referência de RNF-009** para que um arnês sirva aos dois. Com eles, **SCN-003.4**, sem o qual RN-039 seria regra invisível à verificação. Procedência informada 52 → 54; 36 RN, 11 RNF, 66 cenários. **`/techspec`:** um DR novo, **SDR-007**, e nenhuma devolução consumida — o escopo novo nasceu no `/prd` e chegou aqui com cenário e procedência. O recorte é aplicado **na consulta**, contra a coluna projetada `tarefa.tornou_se_terminal_em`, e **não derivado em tempo de leitura**: derivá-lo exigiria agregação por tarefa sobre `evento_tarefa`, a maior tabela do sistema, encarecendo a leitura na exata proporção da história que o recorte existe para esconder. A coluna é anulada na reabertura, é determinada pelo log e por isso **curável** por SDR-006, ao contrário de `raia_id` e `descricao`; entra com índice parcial e migration retroativa de ordem 8. **ACH-02 é o achado que organiza os outros:** a raia sintética estava justificada — cartão que não cabe na grade some, e some **sem erro** — e a frase inteira se aplicava à etapa arquivada, onde não havia defesa alguma, e por caminho **permitido**, já que a contagem que barra o arquivamento exclui declaradamente as condições terminais. Passa a haver faixa sintética nos dois eixos sob regra única — existe quando, e só quando, há cartão que precise dela —, o que resolve junto a variação de comprimento que a suíte indexava por posição. **ACH-01:** `acessoPorAdministracaoGlobal` passa a ser declarada no corpo do board, onde a suíte congelada já a exigia. **ACH-05 e ACH-08** eram o contrato contradizendo a si mesmo sobre a forma do cartão: `permanencia` ganha `| null`, porque permanência é intervalo aberto e tarefa terminal não tem nenhum, e `assumidaEm` entra no cartão, onde as duas promessas do próprio contrato já o pressupunham. Validação do PRD em **exit 0**; a da TechSpec com **um erro, e é a pendência 17** |
| 2026-09-16 | **`/tasks` — a rota órfã tem dono, e a última questão da TechSpec sai da fila.** Nasce **TASK-02.10 — ficha da tarefa**, fechando ACH-02 de TASK-02.6: `GET /v1/tarefas/{tarefaId}` é declarada pelo contrato desde a v1.0 e exercitada por **onze classes** da suíte congelada, e não pertencia a task nenhuma — `state.md` a tratava como superfície de TASK-02.6, que entrega só o board. A task foi escrita a partir das asserções congeladas e não do contrato apenas, porque a suíte é mais específica que ele: `etapaNome`, `episodioAtual`, `log` em ordem cronológica com `episodio` por evento, e **`ator` omitido e não nulo** quando o evento não tem ator. Ela ficou em **EPIC-02 e não em EPIC-03** por uma razão aritmética e não temática: é ela que torna mensuráveis os critérios já em aberto de TASK-02.4 (ACH-05) e TASK-02.5 (ACH-04), ambos do mesmo épico, e **os dois achados foram fechados como achados de plano** — não havia conserto dentro do escopo de arquivo daquelas tasks, e agora há endereço. Nasce também **TASK-01.9**, distribuindo **Q-012** (claim ausente, decidida na TechSpec v1.8 e sem task desde 2026-09-10): declarada **fora do gate de EPIC-01**, porque o épico está fechado e a dívida vence por evento externo — o primeiro realm federado — e não por posição na fila; a migration recebe ordem nova, já que a de ordem 7 que a v1.8 previa foi consumida pelo fechamento de ACH-02 de TASK-02.3. Fechados ainda ACH-01 e ACH-03 de TASK-02.6 (dois arquivos alterados fora da tabela, e a contagem de consultas corrigida de quatro para cinco de projeção mais a leitura do projeto, com a **raia sintética** passando a constar do artefato — era decisão de desenho que não estava escrita em lugar nenhum) e ACH-13 de TASK-02.5, cujo ponto de atenção mandava implementar `long contarEm(UUID)`, forma anterior a ACH-07 da reexecução de TASK-02.2. `RaiaRepositorio.java` entrou na tabela de TASK-02.5. Corrigida uma contradição antiga do próprio plano: a prosa dizia que SCN-002.4 fica no EPIC-01 e a tabela de cobertura sempre disse EPIC-02 — vale a tabela, sem mudança de atribuição. **`check_cobertura.py` em exit 0**; o `validate.py` sai com os erros de pendência 11 mais **três novos, que são pendência 24**: o validador exige cenário por task, e a invariante do gate é cenário por **épico** |
| 2026-09-15 | **TASK-02.6 — a leitura do board nasce, e ACH-04 de TASK-02.5 deixa de ser o que era.** Três arquivos, exatamente os declarados. A grade é o produto cartesiano etapa × raia montado em memória a partir de **cinco consultas fixas**, todas por projeto e nenhuma por cartão — a junção em memória é escolha e não acomodação: uma consulta única com quatro junções devolveria o produto cartesiano das séries, e desdobrá-la custaria o mesmo com o tráfego multiplicado. A task fala em quatro porque conta etapa e raia como uma; o critério exige número **fixo**, e é o que ele é. **A decisão de desenho que não estava escrita em lugar nenhum é a raia sintética** (`id = null`): raia é organização opcional por RN-023, de modo que o projeto que nunca configurou nenhuma precisa desenhar mesmo assim — e o cartão sem raia, ou apontando para raia arquivada, precisa de faixa, sob pena de **sumir do board sem erro**, que é a pior forma de perder trabalho em curso. Não há filtro por `condicao`: `ReaberturaIT` prova que cartão concluído aparece na etapa terminal; o que o board exclui é etapa arquivada. **Medição: 198 testes, 117 verdes / 81 vermelhos**, contra 198 / 110 / 88 — **+7 verdes, zero regressão**, e a única classe suspeita foi conferida rodando contra a árvore do `HEAD`, onde já falhava pelo mesmo motivo. **Cinco dos sete critérios medidos e satisfeitos:** 2 e 4 pela suíte congelada, e **5, 6 e 7 por sonda descartável fora dela**, pelo recurso que TASK-02.3 e TASK-02.5 já usaram — **11 *prepared statements* com 5 tarefas e os mesmos 11 com 50**, que é a prova direta de que o número não cresce; `404` tanto para quem não participa quanto para projeto inexistente; `seq` do board igual ao `max(seq)` do log. **1 e 3 seguem sem medição por dependência de EPIC-03/04**, e é a mesma razão de sempre. **Um defeito real apareceu na medição e foi corrigido:** mapa imutável recusa consulta por chave nula, e a chave da raia sintética é nula — o board vazio saía `500`, e nenhuma leitura de código teria pego isso. **Três achados, todos com destino `/tasks`:** `CartaoResposta.java` e `CriacaoDeTarefaService.java` alterados fora da tabela de arquivos, porque a suíte congelada exige `responsavel` como objeto `{id, nome}`; a divergência do texto quanto ao número de consultas; e o relevante — **`GET /v1/tarefas/{tarefaId}` não é declarada por task nenhuma**, embora dezenas de vermelhos morram nela e este `state.md` a tratasse como superfície desta task |
| 2026-09-15 | **ACH-07 fechado pelo `/techspec` — contrato do cartão, TechSpec v1.17.** O cartão de `contracts/board-e-tarefas.md` não declarava `etapaId`, e a omissão tem causa identificável: o campo foi pensado de dentro do board, onde a etapa é dedutível do aninhamento em `etapas[].raias[].tarefas[]`. Mas o cartão é o **mesmo objeto** que sai de `POST /tarefas`, dos movimentos, da tomada e de `GET /tarefas/{id}` — e nesses não existe lista de onde deduzir. O custo não era estético: toda escrita seguinte sobre a tarefa exige `origem: { etapaId, condicao, versao }` por SDR-002, de modo que sem o campo o cliente teria de **reler o board** depois de criar ou mover uma tarefa só para poder escrever de novo, transformando o mecanismo de concorrência em ida e volta obrigatória. A suíte congelada já exigia o campo (`CriacaoDeTarefaIT` afirma `$.etapaId`) e TASK-02.5 já o emite: **a divergência era do contrato, não da implementação** — e é esse contrato que TASK-02.6 reusa para o board. Validação com um erro, e é a pendência 17 (linha 244, menção a TASK-02.2) |
| 2026-09-15 | **Os 3 bloqueantes de código de TASK-02.5 fechados, e os dois relevantes de borda com eles — medido.** Os três tinham a mesma forma, que é a tese da revisão: **o que o cliente manda não era conferido contra o escopo da rota**. `raiaId` passa a ser recusado com `422 raia-fora-do-projeto` quando não pertence ao projeto da rota, quando não existe e quando está arquivada — as três pelo mesmo `EXISTS` escopado, porque as três produzem o mesmo dano e distinguir na mensagem diria a quem tentou se a raia do outro projeto existe. **A FK não podia fazer esse papel:** `tarefa.raia_id` referencia `raia (id)` sem escopo de projeto, e o arquivamento é lógico, de modo que ela aceita as duas primeiras e nunca vê a terceira — pertencimento e vigência são regra, e regra não cabe numa chave estrangeira (ACH-01, ACH-02). O método nasce no repositório **com o consumidor**, como `EtapaRepositorio` institui, e devolve booleano de propósito: carregar a linha daria acesso a dado de outro projeto justamente no caminho que existe para recusá-lo. ACH-03 saiu com o bloqueio pessimista da linha de `projeto` **antes** da leitura do fluxo, cumprindo a cláusula de SDR-005 nesta rota como a de substituição já cumpre — e **não introduz ponto de contenção novo**, porque toda escrita de tarefa já serializa nessa mesma linha no `UPDATE ... seq_atual` de SDR-004; o que muda é *quando* o bloqueio é tomado, e a ordem de aquisição é a mesma das duas rotas, sem ciclo. Com ele veio o `timeout = 10` que faltava (ACH-11): é o bloqueio que torna a espera possível, e teto de espera sem teto de transação é meia garantia. ACH-05 fechou com tetos **de campo** e não herdados do corpo — 200 para `titulo`, 4000 para `descricao`, recusa antes de qualquer escrita —, e isso fecha **ACH-06 de brinde**: título acima de 8 KiB era recusado lá adiante pelo teto de `dados` do núcleo, como `400` genérico e depois de a linha de `tarefa` já ter sido gravada. **Medição por sonda descartável no contêiner, fora da suíte congelada:** raia de outro projeto, raia arquivada, raia inexistente, título de 9 000 e descrição de 9 000 saem **`422`** com o campo nomeado em `errors`, e raia legítima do próprio projeto continua **`201`**. **Suíte: 198 testes, 110 / 88 — idêntica à medição da revisão, zero regressão.** **ACH-04 segue aberto e não é desta mão** — cinco critérios sem medição por dependência das rotas de TASK-02.6, dono `/tasks`. Fica registrado que `RaiaRepositorio.java` **não consta da tabela de arquivos da task**: o conserto do achado o exigiu |
| 2026-09-15 | `/code-review TASK-02.5` — **14 achados, 4 bloqueantes, e o rebaixamento de TASK-02.4 caducou sem dívida.** As duas metades que eram desta task fecharam como a emenda previu, **estruturalmente**: `NovaTarefaRequisicao` não tem campo de ator e `dados` é construído chave a chave, de modo que não há caminho do corpo ao log — ACH-12 e ACH-15 de TASK-02.4 confirmados fechados por leitura independente dos agentes `security` e `qa`, que chegaram aos mesmos achados novos por caminhos separados. O que a borda trouxe de novo são três defeitos de produção e todos têm a mesma forma — **o que o cliente manda não é conferido contra o escopo da rota**. `raiaId` é gravado sem conferir que a raia pertence ao projeto nem que não está arquivada, e a FK é global: quem escreve no projeto A cria tarefa apontando para raia de B, e o UUID inexistente sai como `5xx` porque o tradutor de restrição é nominal e só conhece a unicidade de ordem de etapa. O terceiro é o mais sutil e é **a cláusula de SDR-005 aplicada pela metade**: o fluxo é lido antes de a transação tomar bloqueio algum — o bloqueio de linha só chega no `UPDATE ... seq_atual`, lá dentro do registrador —, de modo que um `PUT /etapas` concorrente conta zero tarefas na etapa, a arquiva, e a criação insere em etapa arquivada, tarefa que o board não mostra e que RN-005 não alcança. É exatamente o que ACH-04 da reexecução de TASK-02.2 fixou para a outra rota. O quarto bloqueante é o de sempre e não é desta mão: **cinco dos dez critérios sem medição**, porque morrem nas rotas de leitura de TASK-02.6 — inclusive 7 e 8, que verificariam a porta que esta task existe para publicar. Entre os relevantes, `titulo` e `descricao` sem teto na borda (o de 8 KiB do log faz o papel por acidente, e devolve `400` genérico depois de a linha de `tarefa` já ter sido gravada), o contrato do cartão sem `etapaId` que a suíte congelada exige, e **dois testes rotulados `SCN-004.3` que verificam outra coisa** — o cenário é projeto sem fluxo, e o que eles exercitam é a recusa por papel somente-leitura. **A suíte foi remedida no contêiner de ADR-012 e confere com o `/implement` sem divergência: 198 testes, 110 / 88** — o Docker Desktop estava na máquina fora do `PATH`, mesmo padrão do Maven em `D:\CobraKai`. **E dois achados deixaram de ser leitura:** sonda descartável em cópia fora da suíte congelada mede `raiaId` de outro projeto saindo **`201 Created`**, com o identificador alheio devolvido no cartão, e `raiaId` inexistente saindo **`500`**. ACH-01 e ACH-02 passam de plausíveis a confirmados. Os 7 erros de `check_veredicto.py` são sinal correto. Ver `docs/review/kanban-tarefas/TASK-02.5-review.md` |
| 2026-09-15 | **ACH-03 e ACH-04 fechados pelo `/tests` — plano de verificação v1.9 —, e com eles o critério 6 deixa de ser insatisfazível.** Os dois estavam na suíte congelada. ACH-03: `Cenario.recuarInicioDoIntervalo` desloca `intervalo_tarefa.inicio` e nunca `evento_tarefa.ocorrido_em`; para os vinte e oito pontos que o usam isso é inofensivo, porque leem a projeção, mas para `ReconstrucaoDaProjecaoIT` é fatal, porque lá a comparação é entre a projeção gravada e o que o log sustenta — **a divergência era fabricada pelo arnês e apareceria como defeito da rotina**. **Não existe conserto do método que preserve a semântica dele**, e isso ficou escrito: os intervalos de uma tarefa se sobrepõem (RN-008) e `TAREFA_MOVIDA` fecha e abre `PERMANENCIA` no mesmo instante, de modo que deslocar o log coerentemente exige transladar um prefixo inteiro — e prefixo não produz recuos independentes por intervalo, dois recuos seguidos compõem e as durações afirmadas pelos testes de leitura deixariam de valer; deslocar só o evento que abre o intervalo é pior, porque o mesmo evento fecha o anterior e encurtá-lo produz duração negativa. A correção é no ponto de uso: o método fica, agora **declarado no Javadoc como fixture do modelo de leitura e só dele**, e nasce `envelhecerProjeto`, **translação rígida** de todo evento, toda fronteira de intervalo e `assumida_em` do projeto — log e projeção se movem juntos e nada é fabricado. Perder as durações infladas dos intervalos fechados não custa poder de falha, porque a asserção principal compara o dump completo da série de tempo. ACH-04: `TRUNCATE intervalo_tarefa` é recusado pela FK `NOT NULL` de `impedimento`, **medido** contra `postgres:16-alpine` com as nove migrations aplicadas; `CASCADE` foi recusado por razão de desenho e não de conveniência — por SDR-006 a rotina **não cria linha de `impedimento`**, e apagá-la junto exigiria da rotina o que ela declaradamente não promete. O método vira `corromperAProjecao`: apaga toda linha que nenhum impedimento referencia e, nas que sobram, corrompe `inicio` e anula `fim`, que são justamente os dois campos que o log determina. **`test-compile` no contêiner de ADR-012 com os mesmos três arquivos já conhecidos e nenhum erro novo**, e as cinco sentenças SQL novas validadas contra o esquema real. O critério 6 **continua sem medição**, pela razão de sempre — a classe morre em `404` da rota de TASK-02.5 —, mas passou a ser decidível pelo mérito da rotina. **Com isso TASK-02.4 fica com um único achado em aberto: ACH-05, dono `/tasks`** |
| 2026-09-15 | **ACH-02 fechado pelo `/techspec` — SDR-006, TechSpec v1.16.** A §9 de `data-model.md` prometia reescrever `tarefa`, `intervalo_tarefa` e `impedimento` **do zero**, e o log não sustenta a promessa. Três lacunas de naturezas distintas: `raia_id` não existe em evento nenhum, e **não por esquecimento** — §5 declara que é justamente a ausência da raia na série de tempo que torna a agregação por raia impossível, de modo que acrescentá-la ao log para viabilizar a reconstrução **desfaria RN-023**; `descricao` não tem evento que a registre, porque não há cenário congelado de edição de descrição, e inventá-lo aqui seria requisito nascido na TechSpec; e `impedimento.id` é UUID gerado que o log não carrega, enquanto `intervalo_tarefa` é referenciada a partir dele por FK. A decisão foi **estreitar a promessa**, e o que a decidiu foi regra de negócio e não dificuldade. SDR-006 dá à rotina dois alcances: **total** em `intervalo_tarefa`, porque a série de tempo é integralmente determinada pelo catálogo de eventos; **parcial e sobre linha existente** em `tarefa` e `impedimento`, onde ela reescreve só os campos que o log determina e **não cria nem remove linha** — o conjunto de tarefas de um projeto não é saída da rotina, é insumo. É o que a implementação de TASK-02.4 **já fazia por imposição de esquema**: foi a FK não adiável de `impedimento.intervalo_id` que a obrigou a casar linha gravada com linha reexecutada e reescrever no lugar. O esquema já proibia o "do zero"; a spec é que não sabia. A promessa estreitada é também a que SDR-001 sempre fundamentou — o título daquele DR é "projeção **de intervalos**", e é na série de tempo que o risco mora, porque é ela que se corrompe em silêncio; `titulo` errado aparece na primeira tela. O limite fica declarado: `raia_id`, `titulo` e `descricao` corrompidos **não são curáveis** por esta rotina. Na mesma emenda, §4 ganhou o **formato de `dados` por tipo de evento**, fechando a metade documental de ACH-15, e o `quickstart.md` — que é o que o `/implement` lê — ganhou o alcance de SDR-006 e a assimetria do bloqueio consultivo |
| 2026-09-15 | **TASK-02.5 — a borda de escrita nasce, e o rebaixamento dos cinco achados de segurança caduca sem dívida.** Antes de implementar, a task foi **emendada**: três obrigações venciam nela e nenhuma estava escrita ali — viviam no relatório de revisão de TASK-02.4 e em dois Javadoc de `RegistradorDeEvento`. É o padrão de ACH-10 na revisão de TASK-02.2, cuja emenda já registrou que **histórico de outra task não é instrução desta**; sem a emenda o rebaixamento caducaria sem que nada no artefato que o `/implement` lê mandasse fechá-lo, e **nenhum teste acusaria**, porque a suíte congelada não conhece essas metades. **As duas metades fecharam estruturalmente, e não por checagem.** `NovaTarefaRequisicao` não tem campo de ator — o controlador extrai a identidade do principal autenticado e a entrega ao serviço como argumento, de modo que não há o que conferir porque não há o que divergir (ACH-12); e `dados` é **construído** chave a chave a partir do que `data-model.md` §4 declara para o tipo, em vez de repassar objeto recebido, de modo que nenhum caminho leva do corpo ao log imutável (ACH-15). Os dois Javadoc que apontavam para esta task saíram e foram reescritos como a garantia que passou a existir — comentário que aponta para dívida paga é pior que comentário nenhum. **Duas decisões impostas pela suíte congelada:** o serviço tem dois colaboradores no construtor, e por isso o aplicador de intervalos, o `EntityManager` e o conversor entram por campo — sem perda, porque a recusa que o teste unitário exercita acontece antes de os três serem tocados; e a linha de `tarefa` é gravada e descarregada **antes** do evento, porque o núcleo confere que a tarefa pertence ao projeto declarado e precisa encontrá-la. **Uma decisão própria, e ela é de SDR-006:** `TAREFA_CRIADA` grava `etapa_destino_id`, porque sem essa coluna a reconstrução reabre a permanência inicial na etapa **de hoje** — o aplicador cai no estado corrente quando o evento não declara etapa —, e a série de tempo de toda tarefa que se moveu ficaria errada na origem. **Medição: 198 testes, 110 / 88**, contra 190 / 96 / 94: zero regressão, e os arquivos de teste que não compilam caem de 3 para 2. Todo vermelho remanescente morre em `GET /v1/tarefas/{id}` ou `GET /board`, rotas de TASK-02.6. **Critérios 2, 4b, 5, 9 e 10 medidos e satisfeitos** — 9 e 10 direto contra o log em cópia descartável, porque nenhum teste congelado os alcança: `ator_id` é exatamente o identificador de quem autenticou, e `dados` é `{"titulo": ...}` e nada além. **7 e 8 seguem sem medição** pela mesma dependência de leitura. **Achado aberto:** o ponto de atenção da task manda implementar `long contarEm(UUID etapaId)`, e a porta real é `Map<UUID, Long> contarEm(Collection<UUID>)` desde ACH-07 da reexecução de TASK-02.2 — a task está desatualizada, destino `/tasks` |
| 2026-09-15 | **ACH-02 fechado — TechSpec v1.16, SDR-006.** A `data-model.md` §9 prometia que a reconstrução reescreve `tarefa`, `intervalo_tarefa` e `impedimento` **do zero**, e o log não sustenta: `raia_id` não existe em evento nenhum, `descricao` não tem evento que a registre, e `impedimento.id` é UUID gerado que o log não carrega — enquanto `intervalo_tarefa` é referenciado por FK a partir dele. **O que decidiu não foi a dificuldade e sim o custo em regra:** registrar a raia na série de tempo **desfaz RN-023**, porque a §5 deste mesmo documento declara que é a ausência dela ali que torna a agregação por raia impossível. Ampliar o log para cumprir a promessa pagaria uma regra de negócio por uma rotina administrativa. A promessa passa a ter **dois alcances** — total para `intervalo_tarefa`, que o catálogo de §4 determina inteiramente, e parcial sobre linha existente para `tarefa` e `impedimento`, com a tabela de campos reescritos e preservados escrita no lugar; a rotina não cria nem remove linha nessas duas. **Era o que a implementação já fazia por imposição do esquema, sem que a spec soubesse:** foi a FK de `impedimento` que obrigou TASK-02.4 a casar linha gravada com linha reexecutada em vez de apagar e inserir. O limite fica declarado e é real — `titulo`, `descricao` e `raia_id` corrompidos não são curáveis pela rotina, e tarefa presente no log e ausente de `tarefa` é divergência reportada e não corrigida. Com isso **o critério 6 deixa de ser insatisfazível por implementação correta nenhuma**. Na mesma emenda, `dados` ganha **formato declarado** — um objeto por tipo de evento, chaves nomeadas, teto de 8 KiB, `descricao` explicitamente fora —, porque a reconstrução é o primeiro leitor da coluna e leitor sem formato é acoplamento ao que o escritor fez naquele dia; fecha também a metade documental de ACH-15. Validação com **um erro, e é a pendência 17** |
| 2026-09-15 | **Os cinco achados de segurança de TASK-02.4 rebaixados, com aprovador humano nomeado (Thiago Goncalves Cavalcante).** ACH-12, ACH-13 e ACH-15 a relevante; ACH-16 e ACH-17 a menor. Eles eram bloqueantes **por regra e não por gravidade** — achado de segurança é bloqueante por padrão —, e a justificativa registrada é única: os cinco estão fechados na parte que o escopo de arquivo da task alcança, e o que resta de cada um depende da **borda, que não existe**. Sem rota, não há superfície por onde dado de cliente ou principal divergente entre, e **nenhum envelope fica descoberto**: RNF-008 segue com as duas pernas, porque ACH-17 alcança a projeção — descartável por SDR-001 — e nunca o log; RNF-002 era o que ACH-12 ameaçava, e essa metade fechou quando a coerência tarefa↔projeto passou a ser conferida antes de a sequência ser consumida; RN-008 e RN-014 não são alcançadas por nenhum deles. **O argumento tem prazo, e o precedente mostra que o prazo é real:** o rebaixamento de ACH-08 em TASK-02.3 dizia "não há escritor" e **caducou em TASK-02.4**, que é o primeiro escritor; este diz "não há chamador externo" e **caduca em TASK-02.5**, que cria a borda. As metades abertas — `atorId` contra o principal autenticado, e distinguir dado de cliente de dado de serviço em `dados` — são obrigação daquela task, não dívida perdoada. Com isso TASK-02.4 fica com **4 bloqueantes**, nenhum deles desta mão: ACH-02 (`/techspec`), ACH-03 e ACH-04 (`/tests`) e ACH-05 (`/tasks`) |
| 2026-09-15 | **TASK-02.4 medida, e ACH-05 troca de natureza.** O Docker estava na máquina o tempo todo, fora do `PATH` — mesmo padrão do Maven em `D:\CobraKai`. A suíte rodou inteira no contêiner de ADR-012: **190 testes, 96 verdes / 94 vermelhos**, contra 189 / 96 / 93. O teste a mais é o arquivo a mais que passou a compilar e sai vermelho pela mesma dependência das demais; **zero regressão**. **Os sete critérios de aceite continuam sem medição, e a razão deixou de ser ambiente:** `SeqSobConcorrenciaIT`, `ReconstrucaoDaProjecaoIT` e `ImutabilidadeDoLogIT` morrem antes da primeira asserção, em `404` de `POST /v1/projetos/{id}/tarefas`, rota que nasce em **TASK-02.5** — a falha `Expected size: 40 but was: 0` do critério 1 é ausência de escrita e não defeito do mecanismo. ACH-05 passa de pendência de ambiente a dependência de task, e o destino muda de `/implement` para `/tasks`: não há conserto possível dentro do escopo de arquivo desta task. **O mecanismo de ACH-01 foi medido, ainda que o critério não** — pelo mesmo recurso que TASK-02.3 usou quando a suíte não alcançava, direto contra `postgres:16-alpine`: duas sessões tomam `pg_try_advisory_xact_lock_shared` na mesma chave e **ambas recebem `true`**, e a exclusiva sobre a mesma chave, com um compartilhado vivo, recebe `false`. É a assimetria que o desenho exige, e prova que a correção de uma palavra faz o que promete. Testada e **falsa** a hipótese de que o gate administrativo de ACH-17 teria quebrado a reconstrução: zero `AccessDeniedException` na suíte inteira. Fica registrado o que custou uma execução inteira de download: o contêiner rodou **sem montar o `.m2`** |
| 2026-09-15 | **Os 13 achados de código de TASK-02.4 fechados; 6 seguem abertos, e nenhum deles é desta mão.** O bloqueante ACH-01 saiu numa palavra: o escritor passa a tomar o bloqueio consultivo em modo **compartilhado**, e o reconstrutor segue no exclusivo. A relação que o desenho sempre exigiu é assimétrica — escritores convivem entre si e nenhum convive com a reconstrução —, e era essa assimetria que faltava expressar; com os dois lados exclusivos, o critério 1 falhava por construção e qualquer participante do projeto impedia os demais de escrever só mantendo transações abertas. **ACH-06 e ACH-19 fecharam pela mesma mudança**, que vale registrar porque é o tipo de dependência que não aparece até quebrar: o `UPDATE ... RETURNING` foi envolvido em `WITH ... SELECT`, porque o Hibernate classifica a consulta pelo verbo inicial e ler o resultado de um comando que ele considera de escrita funciona por acidente e não por contrato — e o ponto de que todo o caminho de escrita do sistema depende não pode depender de comportamento não especificado de uma versão de biblioteca. De brinde, projeto inexistente passou a devolver zero linhas e sair como `404` em vez de `500`. **Dois achados fecharam pela metade, e as metades que faltam estão escritas no código, não perdidas:** a coerência tarefa↔projeto agora é conferida antes de tudo — sem ela um evento com o `projetoId` alheio grava no log de outro projeto **e consome a sequência dele**, produzindo lacuna permanente e indistinguível de mensagem perdida no board de quem nada fez —, mas conferir `atorId` contra o principal autenticado acoplaria o caminho único de escrita ao contexto de segurança web e o quebraria para o job e para o arnês, que legitimamente não têm principal; e a validação de `dados` ganhou teto e checagem de JSON, sem que isso distinga dado de cliente de dado de serviço, que não é validação de forma. Os dois pertencem à borda, que nasce em TASK-02.5. O gate administrativo do reconstrutor (ACH-17) inverte o usual de propósito: **ausência de autenticação autoriza**, porque a rotina é de processo; o que ele impede é chegar ali por dentro de uma requisição autenticada qualquer. Mais: exceção no `afterCommit` morre no gancho e vira log, porque `500` sobre escrita já comitada convida a repetir o que, num log imutável, grava de novo; o log passa a ser reexecutado em lotes de 500 com `em.clear()` entre eles, seguro ali e só ali; remoção do excedente em lotes de 1000; a chave do bloqueio sai de SHA-256 e não do `xor` das metades, que era construtível; `condicaoOrigem`/`condicaoDestino` viram enum; e os dois textos que afirmavam a perna de banco de RNF-008 fora de vigor foram corrigidos, porque TASK-02.9 a pôs em vigor no commit anterior. **Medição: 28 unitários verdes, zero regressão, os mesmos 3 arquivos de teste não compilando e nenhum novo.** O Docker segue ausente, de modo que **ACH-05 continua aberto e com ele a verificação de que ACH-01 de fato passou** — a correção está argumentada, não medida. Os outros cinco abertos: ACH-02 (`/techspec`), ACH-03 e ACH-04 (`/tests`, os defeitos da suíte congelada), ACH-11 e a conferência de `atorId` (`/tasks`) |
| 2026-09-15 | `/code-review TASK-02.4` — **19 achados, 10 bloqueantes**, e **um só é defeito de produção**. ACH-01: o escritor toma `pg_try_advisory_xact_lock` **exclusivo** sobre a mesma chave do reconstrutor, de modo que escritores se excluem entre si — a segunda escrita concorrente no mesmo projeto sai `409 reconstrucao-em-curso` sem reconstrução nenhuma em curso, e o critério 1 falha por construção. O desenho exige modo compartilhado do lado de quem escreve e exclusivo só do lado que reconstrói; o conserto é do `/implement`. O agente `security` chegou ao mesmo achado por caminho independente, como sua descoberta nº 1, e acrescentou o ângulo que faltava: **qualquer participante do projeto passa a poder impedir os demais de escrever** só mantendo transações abertas, sem privilégio algum além do que já tem, com `idle_in_transaction_session_timeout` de 30 s como único teto. Dois bloqueantes são da **suíte congelada** e foram confirmados pelo revisor — `recuarInicioDoIntervalo` desloca só a projeção e nunca o log, tornando o critério 6 insatisfazível por implementação correta nenhuma, e o `TRUNCATE intervalo_tarefa` colide com a FK de `impedimento` —, um é de spec (§9 promete reconstruir o que o log não carrega) e um é a **ausência de medição dos sete critérios**, que reprova o GATE-NFR por omissão. Os outros cinco entram como bloqueantes **por regra e não por gravidade aferida**: são achados de segurança, e rebaixar exige aprovador humano nomeado, que o revisor não pode ser — coerência não conferida entre `tarefaId`/`projetoId`/`atorId` no caminho único de escrita, exceção no `afterCommit` virando `500` sobre escrita já comitada, `dados` livre num log imutável (o rebaixamento de ACH-08 de TASK-02.3 **caducou aqui**, porque esta task é o primeiro escritor), `chaveDeBloqueio` por `xor` construtível e o reconstrutor público sem autorização. Nenhuma injeção, nenhum vazamento em mensagem de erro, nada de `dados` no broadcast. Os 8 erros de `check_veredicto.py` são sinal correto. Ver `docs/review/kanban-tarefas/TASK-02.4-review.md` |
| 2026-09-15 | **TASK-02.4 — o núcleo de escrita, e a primeira task do épico que ninguém consegue medir.** Sete arquivos novos e quatro alterados: a sequência vem de `UPDATE projeto SET seq_atual = seq_atual + 1 ... RETURNING` **dentro** da transação (SDR-004), o evento entra por `EntityManager.persist` e não por `save` de repositório, a projeção é escrita na mesma transação e a publicação sai em `afterCommit` por uma porta cuja única implementação hoje é silenciosa. O catálogo de evento→intervalo da seção 4 está transcrito **uma vez só**, e essa unicidade é o ponto: duas cópias fariam a projeção reconstruída divergir da gravada por diferença de tabela, e a divergência apareceria como defeito da rotina de reconstrução — o último lugar onde alguém procuraria. **Três decisões que custaram análise.** A transação da reconstrução é `TransactionTemplate` e não `@Transactional`, porque `reconstruirTudo` chama `reconstruir` pelo `this`: sem proxy, a anotação é ignorada e o bloqueio `xact` solta no fim da própria consulta que o toma, apagando a janela sem escrita **sem que nada falhasse**. A reconstrução não pôde ser apaga-tudo-e-insere, porque `impedimento.intervalo_id` é FK não adiável — ela casa linha gravada com linha reexecutada por `(tarefa_id, tipo)`, reescreve no lugar, apaga o excedente e só então insere o que falta. E quem chama o aplicador é o **serviço de domínio** e não o registrador, ao contrário do que a tabela de arquivos da task diz: a suíte congelada constrói `TomadaService` e `ImpedimentoService` com o aplicador como colaborador e estipula `registrar` com um argumento, e suíte congelada está fora do alcance do `/implement`. **Medição:** `compile` verde e 28/28 unitários, zero regressão; **os sete critérios de aceite sem medição**, por ausência de Docker. **Cinco achados**, dois deles bloqueantes e ambos na suíte: `Cenario.recuarInicioDoIntervalo` desloca só `intervalo_tarefa.inicio` e nunca `evento_tarefa.ocorrido_em`, de modo que a igualdade exata do critério 6 **não é satisfazível por implementação correta nenhuma**; e `apagarAProjecao()` trunca `intervalo_tarefa` enquanto a FK de `impedimento` a referencia, o que o PostgreSQL recusa. Os outros três: a seção 9 da TechSpec promete reconstruir `tarefa` e `impedimento` do zero, e o log não carrega `raia_id`, `impedimento.id` nem o formato de `dados` — destino `/techspec`; mais a delegação divergente e os dois enums fora da tabela de arquivos |
| 2026-09-15 | **TASK-02.9 — a role de aplicação existe, e com ela RNF-008 deixa de ser promessa.** Decisão do demandante sobre ACH-01 de TASK-02.3. A aplicação conecta como `kanban_app`, role de login membro de `aplicacao_kanban`, sem `SUPERUSER` e sem posse de objeto; o dono do schema continua migrando, porque migrar é ato de dono (ADR-011). As concessões restritas escritas em TASK-02.1 e TASK-02.3 passam a surtir efeito pela primeira vez: contra superusuário e contra dono, `REVOKE` é decoração. **A contradição da suíte (pendência 22) não foi desatada relaxando privilégio, e sim reconhecendo que preparar o ambiente e exercer o produto são papéis diferentes** — `esvaziarBanco` abre conexão própria com a credencial do dono, e só a aplicação usa a restrita. A senha nunca entra em SQL versionado: vai por callback `afterMigrate` com placeholder do Flyway, alimentado pelo segredo montado em produção e pela constante do arnês no teste; como callback, não entra em checksum e a rotação dispensa `repair`. Descoberto no caminho que `ImutabilidadeDoLogIT.conectarComoAplicacao` **usava o usuário do contêiner** — a verificação de RNF-008 dizia medir a credencial da aplicação e media exatamente o oposto dela. `COMMENT ON ROLE` foi escrito e retirado: exige superusuário, e migration que só aplica sob superusuário contradiz o desenho que ela instala. **Medição:** quatro dos cinco critérios satisfeitos e o segundo satisfeito pela metade por dependência — `aProjecaoContinuaGravavel` verde com a credencial restrita, e a outra verificação vermelha em `404` de `POST /v1/projetos/{id}/tarefas`, rota de TASK-02.5, como as demais classes. O que ela exige foi medido direto contra o esquema em `postgres:16-alpine` limpo: `UPDATE`, `DELETE` e `TRUNCATE` de `evento_tarefa` em *permission denied*, `DELETE` de `etapa` e `raia` idem, `CREATE TABLE` no `public` negado, `INSERT` no log permitido e `DELETE` na projeção permitido. **Zero `permission denied` na suíte inteira — nenhuma classe vermelha no `@BeforeEach`** — e 189 testes, 96 / 93, idêntico à base |
| 2026-09-14 | `/implement TASK-02.3` — o anel de verdade e o de projeção entram no esquema: quatro migrations (`tarefa` com a restrição de domínio de `condicao`, `evento_tarefa` com a concessão restrita, `intervalo_tarefa` e `impedimento` com os únicos parciais, a unicidade de `(projeto_id, seq)` e a `duracao` gerada), quatro entidades e quatro repositórios, nenhum deles publicando remoção física. **Três achados abertos na execução.** O primeiro e o segundo são o mesmo nó: os critérios 4 e 10 — a role de aplicação com apenas `SELECT, INSERT` no log e sem `DELETE` em `etapa`/`raia` — **não são satisfeitos**, porque a role de aplicação distinta continua não existindo; a aplicação conecta como o `POSTGRES_USER` da imagem, que é superusuário e dono, e contra ele todo `REVOKE` é inerte. É o que ACH-09 de TASK-02.1 previu ao recusar fazê-lo lá, supondo que aqui a role existiria. Pior: a suíte congelada **contradiz a si mesma** nesse ponto — `ImutabilidadeDoLogIT` exige que o `TRUNCATE` do log falhe para a credencial da aplicação, e `TesteDeIntegracao.esvaziarBanco` trunca todas as tabelas com **essa mesma credencial** antes de cada teste. Nenhuma configuração de privilégio satisfaz as duas. O terceiro é menor: `impedimento.resolvido_em` entrou no esquema **sem constar** da task nem de `data-model.md` §5, porque `ImpedimentoServiceTest` a exige. **Nada foi executado** — `mvn`, `docker` e JDK sumiram da máquina de novo, e o git recusa o repositório por *dubious ownership*, o que também impede `check_escopo.py` e o commit |
| 2026-09-15 | **Os 7 achados não bloqueantes de TASK-02.3 fechados, e o oitavo virou decisão.** Três mãos. `/techspec` — **TechSpec v1.15**, três emendas em `data-model.md`: `impedimento` ganha `versao`, porque a segunda sinalização de RN-010 é leitura-modificação-escrita sobre o JSON de `anotacoes` e o único parcial não cobre duas escritas no **mesmo** impedimento, só dois impedimentos abertos — a omissão vinha de analogia indevida com `intervalo_tarefa`, cuja escrita é insere-e-fecha; a assimetria de FK passa a ter **regra única e escrita** — referência dentro do anel de projeção é chave estrangeira, sem cascata, porque a tarefa não é removida fisicamente e a restrição existe para impossibilitar a linha órfã e dar à rotina de reconstrução a ordem que ela pressupõe, e referência para fora do anel **não** é, porque `projeto_id` e `etapa_id` são instantâneos históricos e etapa arquivada não pode invalidar a série de tempo que já a atravessou; e o índice da fila perde a coluna-chave `condicao`, constante dentro do próprio predicado parcial. `/tasks` — a task deixa de declarar SCN-004.1, porque **task de esquema não entrega cenário**, e a invariante segue fechada pelas três tasks que já o declaram; os quatro repositórios e `Condicao` entram na tabela de arquivos. `/implement` — migration de ordem 7 em vez de editar as aplicadas, e o `COMMENT ON TABLE` de `evento_tarefa` reescrito porque comentário de `.sql` não viaja para o catálogo e era só a versão que afirma a garantia dupla que chegava a quem lê por `\d+`. **189 testes, 96 / 93 — idêntico à medição anterior, zero regressão —, e o contexto sobe com `ddl-auto=validate` sobre o esquema corrigido**, que é a única prova de que a coluna nova e as duas FK batem com o mapeamento. **ACH-08 rebaixado a menor com aprovador humano nomeado (Thiago Goncalves Cavalcante)**: não há escritor de `evento_tarefa` em caminho nenhum hoje, então não existe superfície por onde dado de cliente entre na coluna; o mecanismo nasce com o primeiro escritor, em EPIC-03. **ACH-01 segue aberto e virou pendência 23**, porque não é execução e sim escolha: criar a role de aplicação com credencial separada para o arnês de teste — fechando 21 e 22 juntas, sob pena de deixar toda a integração vermelha no `@BeforeEach` — ou aceitar RNF-008 garantido só pela aplicação e emendar o contrato que hoje promete a garantia de banco |
| 2026-09-15 | `/code-review TASK-02.3` — **8 achados, 1 bloqueante**, e o bloqueante não é da implementação. O trabalho de esquema está correto e medido: dez critérios medidos, dez satisfeitos, `ddl-auto=validate` verde sobre os dois `jsonb`, o `IDENTITY` e a coluna gerada, zero regressão. O que reprova é que **RNF-008 foi medido fora do envelope** — é o primeiro envelope que esta task tornava mensurável, e a imutabilidade do log está garantida por uma perna só, porque a role de aplicação distinta não existe (ACH-01, pendência 21). O achado **não é corrigível de dentro do escopo de arquivo da task**: destino `/tasks`. Os relevantes: a garantia é afirmada no presente em três lugares onde uma das pernas não vale, e o `COMMENT ON TABLE` — que é o que o catálogo entrega — não carrega a ressalva que o comentário do `.sql` carrega (ACH-02); **`impedimento` não tem bloqueio otimista** enquanto a segunda sinalização de RN-010 é leitura-modificação-escrita sobre o JSON de `anotacoes`, de modo que duas sinalizações concorrentes perdem uma anotação em silêncio — o dano que SDR-002 existe para impedir e que `Tarefa` evita com `@Version`, e a spec não declara a coluna (ACH-03); e a task declara cobrir SCN-004.1, que é verificado por `CriacaoDeTarefaIT` e depende da rota de TASK-02.5 — **task de esquema não entrega cenário** (ACH-04). Os menores: repositórios e `Condicao` fora da tabela de arquivos, assimetria de FK dentro do anel de projeção, coluna-chave constante no índice parcial da fila, e a regra "nunca dado de cliente" em `evento_tarefa.dados` como comentário sem mecanismo. Cinco guardrails extraídos. `check_veredicto.py` sai com três erros, e **os três são sinal correto** e não a pendência 20: o cenário não passa, o RNF está fora, e o rebaixamento do achado de segurança ACH-08 espera aprovador humano nomeado. Ver `docs/review/kanban-tarefas/TASK-02.3-review.md` |
| 2026-09-15 | **TASK-02.3 medida por inteiro — os dez critérios medidos, dez satisfeitos.** O Docker Desktop entrou na máquina e a medição voltou ao contêiner `maven:3.9-eclipse-temurin-25` de ADR-012, o que dispensou o recuo a `release 17`. Foi preciso `--add-host host.docker.internal:host-gateway` mais `TESTCONTAINERS_HOST_OVERRIDE`, senão o Testcontainers anuncia `172.17.0.1` para uma porta publicada no host Windows, que o contêiner irmão não alcança — fica registrado porque volta a morder. **Suíte em 189 testes, 96 verdes / 93 vermelhos**, contra a base 189 / 91 / 98: cinco a mais em verde, nenhuma classe antes inteira verde aparece vermelha. **O critério 2 era o de maior risco e está satisfeito** — o contexto sobe com `ddl-auto=validate` em toda a integração, de modo que os dois `jsonb`, o `IDENTITY` e a `duracao` não mapeada validam contra o esquema real. Os outros nove foram medidos **direto contra o esquema** em `postgres:16-alpine` limpo, porque a suíte congelada não os alcança: as duas classes que os exercitariam falham antes da asserção, em `404` de `POST /v1/projetos/{id}/tarefas`, rota de TASK-02.5. As migrations aplicam em ordem; `IMPEDIDA` é recusada; o segundo impedimento aberto e o segundo intervalo aberto do mesmo tipo colidem nos únicos parciais, e os três tipos abertos convivem (RN-008 no esquema); `intervalo_tarefa` não tem coluna de pessoa nem de total (RN-014) e a `duracao` gerada devolve `01:30:00` para 90 min; zero gatilhos. **4 e 10: a concessão restrita faz o que promete, e isso agora é medido** — com uma role de login fabricada no grupo `aplicacao_kanban`, `SELECT`/`INSERT` no log passam e `UPDATE`/`DELETE`/`TRUNCATE` dele e `DELETE` de `etapa`/`raia` saem em *permission denied*, com o arquivamento lógico preservado. **A pendência 21 não fecha com isso** — ela é exatamente a razão de a role ter precisado ser fabricada. O achado permanece, e a medição só troca "escrito e não verificado" por "verificado e inerte no arranjo real" |
| 2026-09-15 | ~~TASK-02.3 medida pela metade~~ — superada pela linha acima no mesmo dia, quando o Docker apareceu. **TASK-02.3 medida pela metade, e a metade que falta é justamente a dela.** O Maven 3.9.14 estava em `D:\CobraKai` fora do `PATH`, e o `conf/settings.xml` global impunha mirror para um Nexus corporativo que não resolve nesta rede — **removido por decisão do demandante**, com backup em `settings.xml.bak-nexus`; as dependências passam a vir do Maven Central. Com isso: `compile` em BUILD SUCCESS com 46 fontes e **28 testes unitários, 28 verdes**, zero regressão, medidos com `-Dmaven.compiler.release=17` porque o JDK do host é 17 contra o 25 do projeto — aferição, não a build do projeto. Seguem **4** os arquivos de teste que não compilam por dependerem de tasks posteriores, os mesmos de antes: esta task criou as entidades, não os serviços que os testes instanciam. Outros três quebram só pelo recuo a 17 (`List.getFirst()`, `ExecutorService` em *try-with-resources*) e não são defeito. **Docker continua ausente**, e sem ele nenhum teste de integração sobe: os critérios 1, 2, 3, 5, 6, 7 e 9 — todos os que verificam o esquema, que é o conteúdo desta task — **permanecem sem medição**, e com eles o risco dos dois `jsonb` e do `IDENTITY` sob `ddl-auto=validate`. `check_escopo.py` roda pela primeira vez, mas acusa a árvore inteira: o commit `a5b2f79` recolheu num só ponto o trabalho pendente de TASK-02.2 e o desta task. O que é desta task e está fora da tabela de arquivos são os quatro repositórios e o enum `Condicao` — pedidos pelo texto da task, ausentes da tabela dela |
| 2026-09-14 | **Pendências 12, 18 e 19 fechadas, as três medidas no contêiner.** 12 era conversão de caminho: `git diff --name-only` fala da raiz do repositório e a tabela da task fala da raiz do sistema; o prefixo agora vem de `git rev-parse --show-prefix` e o que está fora do sistema sai da lista. 18 **não se reproduziu como descrita** — os scripts retornam 1 e o `validate.py` reprova —, mas o caminho que produz o sintoma existia: `run_custom_steps` classificava pela saída do step, e `ERRO:` com exit 0 virava `AVISO`; linha que se declara erro passa a ser erro, com `on_failure: warn` preservado. 19 **não era edição à mão**: o conteúdo é idêntico após normalizar o fim de linha, e o gerador produzia CRLF no Windows contra LF no Linux — `generate_platform.py` grava em `newline="\n"`, os derivados foram regenerados e `check_drift.py` ganhou `NAO_GERADOS` para o `settings.local.json` da IDE. `check_drift.py` sai em exit 0. Pendência **20** aberta no caminho: `check_veredicto.py` lê a tabela de fechamento como se fosse a de Achados |
| 2026-09-14 | `/tests --audit SDR-005` — `SubstituicaoDeFluxoConcorrenteIT` em `alem/`, a verificação além dos cenários que o DR declara como downstream: dois `PUT` simultâneos deixam o fluxo de **um** deles inteiro e nunca a mistura dos dois, e oito substituições que disputam as mesmas posições não produzem `5xx` nem ordem duplicada. A barreira de largada é o que torna a disputa real — sem ela o custo de subir cada thread serializa as requisições por acidente e o teste passaria verde sobre a implementação que existe para reprovar. **Modo `audit`, e a perda de independência está declarada no plano**: a classe nasceu depois do código e de sua leitura, o que é inevitável quando a decisão vem de achado de revisão. A contramedida foi asserir propriedade de desfecho e nunca mecanismo — trocar o bloqueio por outra coisa que cumpra a promessa mantém os dois verdes. Plano de verificação v1.6; 12 → 13 classes em "além dos cenários". Na mesma versão, **SCN-002.4 ganhou verificador e a cobertura fecha em 70/70** — a pendência 14, aberta desde a v1.1 porque o cenário exigia a tabela `etapa`, que nasceu em TASK-02.1, e o campo `fluxoConfigurado`, emitido em TASK-02.2. Os dois projetos são afirmados na mesma resposta, porque campo constante passaria em qualquer verificação que olhasse um de cada vez. **Sem execução**, pela mesma ausência de toolchain |
| 2026-09-14 | `/implement TASK-02.2 — SDR-005` — o bloqueio pessimista da linha de `projeto` entrou como primeira operação de `substituirFluxo`, antes da leitura do fluxo vigente. A porta ficou em `SubstituicaoDeFluxo.bloquearProjeto` e não em `ProjetoRepository` como o downstream do DR antecipava: `EtapaServiceTest` fixa `new EtapaService(EtapaRepositorio)` e é suíte congelada, de modo que colaborador novo no serviço não cabe. A decisão de SDR-005 é cumprida inteira; muda só a porta. **Sem medição** — não há `mvn`, `mvnw` nem Docker nesta máquina, e as tentativas anteriores mediam dentro do contêiner. Confirmar 168/70/98 é pré-condição da reexecução do `/code-review` |
| 2026-09-14 | **Os 19 achados não bloqueantes fechados — TASK-02.2 sai com 0 em aberto.** Três mãos. `/implement`: teto de corpo em filtro próprio (`LimiteDeCorpo`, `413`), com duas guardas porque `Content-Length` é opcional — `@Size` na lista só é avaliado depois que Jackson materializou o array, de modo que o teto de cem etapas protegia o banco e não a heap; `TarefasAtivasPorEtapa.contarEm` passa a receber a coleção inteira, e a mudança que importa é a **assinatura**, que torna a consulta única possível onde a anterior a tornava impossível, dentro de uma seção crítica com o projeto travado; mais três achados menores de log, mensagem e acentuação. `/techspec`: duas emendas a SDR-005 — a janela TOCTOU da permissão fica **decidida e mantida**, com as quatro condições que a tornam aceitável nesta rota nomeadas para que a próxima decisão não precise redescobri-las, e o `422 etapa-fora-do-fluxo` fica decidido como pretendido e explicitamente não `409`, porque `409` convidaria a retentar o corpo que falhará de novo. `/tests`: **18 testes novos**, doze deles sobre mecanismos que existiam desde o fechamento da primeira revisão sem nenhuma verificação — apagar qualquer um deixava a suíte verde. **A premissa de ACH-11 estava errada, e ficou registrada:** dois corpos com os mesmos `id` são ambos válidos, porque nada foi arquivado entre um e outro; o `422` exige que a primeira requisição **reduza** o fluxo. **Assimetria medida em quatro mutações, todas vermelhas:** sem `liberarOrdens`, sem `and e.arquivadaEm is null`, com o tradutor `409` tornado global, com o teto de corpo elevado acima do corpo de teste. Plano de verificação **v1.8** (13 → 16 classes além dos cenários). Suíte em **189 testes, 91 / 98**, zero regressão |
| 2026-09-14 | **ACH-02, ACH-03 e ACH-04 fechados pelo `/tests` — plano de verificação v1.7.** A correção é de uma palavra e desfaz três achados: as asserções de status de `SubstituicaoDeFluxoConcorrenteIT` passaram de `isLessThan(500)` a **`200` exato**. Serializadas, todas as requisições das duas verificações são válidas e nenhuma pode ser recusada; sem serialização a perdedora sai `409`, pelo tradutor nominal criado na mesma task, e `409` passa por baixo de qualquer faixa. Asserir propriedade do desfecho estava certo — asserir **faixa** de status não. ACH-02 foi fechado por declaração e não por conserto: a mistura dos dois corpos é **inalcançável**, porque as faixas de ordem se sobrepõem e toda intercalação colide antes no índice único parcial, e faixas disjuntas não são alternativa porque o contrato exige ordens contíguas a partir de 1; a asserção fica no arquivo nomeada como rede, não como prova. **Assimetria medida em três execuções:** como está, 2/2 verde; sem `bloquearProjeto`, 2/2 vermelho; com `bloquearProjeto` depois da leitura do fluxo vigente, 2/2 vermelho. A primeira tentativa de mutação de ACH-04 errou o alvo — movi o bloqueio para antes da leitura, não depois — e passou verde por motivo legítimo: o DR exige que ele preceda a **leitura**, não que seja a primeira instrução. Suíte em **171 testes, 73 / 98**, zero regressão. Com isso o épico não tem mais bloqueante aberto |
| 2026-09-14 | **O ambiente deixou de ser bloqueio.** Verificado que o contêiner resolve toda a toolchain ausente do host: JDK 25 e Maven (ADR-012), Testcontainers pelo socket montado, os validadores Python em `python:3.12-slim`, a suíte E2E do Playwright e o perfil `broadcast` de RNF-002 — para os dois últimos falta só copiar `docker/.env.example` para `.env`, já que o segredo `banco-senha.dev` existe. No Git Bash é preciso `MSYS_NO_PATHCONV=1`, senão o `-w` é manglado. **O que o contêiner não resolve:** as pendências 12 e 18, que são defeitos dentro de `check_escopo.py` e `validate.py` e sobrevivem a qualquer ambiente. E `check_drift.py`, rodado pela primeira vez em muito tempo, acusa **9 divergências** em `.github/instructions/` — derivado editado à mão ou fonte alterada sem regenerar. Dívida nova, não ambiente |
| 2026-09-14 | **A toolchain voltou, e a medição confirmou os bloqueantes.** Maven 3.9.14 e Docker 29.7.2 entraram no ambiente; o JDK 25 continua ausente do host e **não faz falta** — a suíte roda em `maven:3.9-eclipse-temurin-25` com o socket do daemon montado, que é o que ADR-012 decidiu. Quatro execuções: `compile` em BUILD SUCCESS com `release 25`; `verify` na árvore íntegra parando no `testCompile` nos **exatos 4** arquivos já registrados, nenhum novo; `verify` em cópia sem esses 4 dando **171 testes, 73/98**, zero regressão, com os 3 testes novos verdes; e o experimento decisivo — `SubstituicaoDeFluxoConcorrenteIT` **com `bloquearProjeto` removido fica 2/2 verde**. O log entrega o mecanismo que ACH-03 previra por leitura: 8 violações de `etapa_projeto_ordem_unico` traduzidas para `409` e absorvidas pelo `isLessThan(500)`. ACH-02, ACH-03 e ACH-04 passam de plausíveis a **confirmados**. Dado não previsto: a classe roda em 0,845 s com o bloqueio e 16,22 s sem ele — vinte vezes, o que mostra que na presença do bloqueio ela não chega a exercitar contenção (ACH-09). `check_escopo.py` segue sem rodar: o `python` do `PATH` é o atalho da Store |
| 2026-09-14 | **ACH-01 fechado**, nas duas mãos que o possuíam. `/implement`: o teto de espera de SDR-005 entrou em três camadas — `lock_timeout` de 5 s na sessão, `@Transactional(timeout = 10)` na rota, `connection-timeout` de 5 s no pool —, e não no hint `jakarta.persistence.lock.timeout`, cujo dialeto PostgreSQL só traduz espera zero: hint positivo seria mais um teto afirmado e não aplicado. Espera esgotada ganhou `503` com `Retry-After` e `type` próprio, porque com o teto a requisição deixa de pendurar e passa a terminar, e o que ela termina não pode sair como `500`; continua `5xx`, o que preserva a exigência da suíte congelada `TransacaoUnicaDeCriacaoIT`. `statement_timeout` ficou fora **declaradamente** — alcança toda consulta e não há medição para escolher o número. `/techspec`: SDR-005 emendado e **TechSpec v1.13**, porque a redação original atribuía o limite a um `timeout` de transação que não existia, e era essa garantia inexistente que sustentava o trade-off. Contrato de `sessao-e-projetos` com o `503`. **Sem execução**: `java` apareceu na máquina, mas é 21 contra o 25 do projeto, e seguem ausentes `mvn`, `mvnw`, Docker, `.m2` local e `python` de verdade |
| 2026-09-14 | `/code-review TASK-02.2` (reexecução) — **23 achados, 4 bloqueantes**, e a maioria sobre verificação e não sobre produção. O conserto dos 14 bloqueantes e relevantes de código da primeira revisão entrou **sem teste algum**: os tetos de `Etapa`, as três recusas de conjunto e o tradutor `409` não aparecem em lugar nenhum da suíte. E a única verificação escrita nesta mão fica **verde com o bloqueio de SDR-005 removido**, por três razões independentes — os dois corpos disputam as mesmas ordens, de modo que a perdedora colide no índice e reverte inteira, e a mistura que o teste afirma detectar é inalcançável; a asserção é `< 500`, que absorve o `409` que o tradutor da mesma task acabou de criar; e a ordem de aquisição, que é a cláusula central do DR, não é verificada. O quarto bloqueante é de produção: `PESSIMISTIC_WRITE` sem `lock timeout`, sem `statement_timeout` e sem timeout de transação, com Hikari no padrão e `db` na probe de readiness — a frase do DR sobre a espera ser limitada pelo timeout vigente descreve algo que não existe. Nada foi executado: não há toolchain na máquina, e a pré-condição "suíte verde" segue inverificável. Ver `docs/review/kanban-tarefas/TASK-02.2-review-r2.md` |
| 2026-09-14 | `/implement TASK-02.2 — achados` — os 14 achados de código fechados, entre eles os 5 bloqueantes desta mão. A forma do **conjunto** passou a ser recusada na borda, antes de qualquer escrita, e os tetos que faltavam viraram validação em vez de suposição: `Etapa.ORDEM_MAXIMA` torna a faixa de trabalho do passo intermediário inalcançável por requisição, `MAXIMO_DE_ETAPAS` e `TAMANHO_MAXIMO_DO_NOME` limitam o que uma escrita autenticada grava. O tradutor de restrição de banco que ACH-02 pedia nasceu **nominal** e não global: o global reprovou `TransacaoUnicaDeCriacaoIT`, que exige `5xx` para violação imprevista — e a exigência está certa, porque violação sem tradutor é defeito e não conflito. 168 testes, 70/98, lista de falhas idêntica à base: zero regressão |
| 2026-09-14 | Os 3 achados de TASK-02.2 sem dono no `/implement` resolvidos pelos donos. **ACH-05 → `/techspec`:** SDR-005 serializa a substituição de fluxo por bloqueio pessimista da linha de `projeto`. SDR-002 não alcançava o caso — o mecanismo de origem declarada pressupõe uma linha existente e versionada pelo cliente, e aqui a unidade é o conjunto: o dano aparece como **ausência**, a etapa que o outro criou e que não está no corpo de quem perdeu, sobre a qual `@Version` não tem o que conflitar. A Alternativa 2 daquele DR, recusada lá por transação longa em board colaborativo, é aceita aqui pelo perfil oposto — operação rara, administrativa, um projeto, teto de 100 etapas —, e trava a mesma linha que SDR-004 já incrementa, sem ponto de contenção novo. Fica **decidido** que a última configuração vence inteira e sem aviso: avisar exige campo de origem no corpo, que quebraria a suíte congelada e é escopo do `/prd`. TechSpec v1.12. **ACH-10 → `/tasks`:** a obrigação de publicar `TarefasAtivasPorEtapa` só existia no histórico da task que criou a porta, e histórico alheio não é instrução — vira item de execução, arquivo declarado e dois critérios de aceite em TASK-02.5, um deles o caso negativo das terminais. **ACH-11 → `/tests`:** a divergência de épico eram **19 cenários e não 3** — EPIC-06 e EPIC-07 estavam trocados em bloco, mais SCN-002.4 e SCN-003.3. A coluna foi alinhada ao plano de tasks, que é a fonte; plano de verificação v1.5 |
| 2026-09-14 | `/code-review TASK-02.2` — 17 achados, 6 bloqueantes. Quatro deles têm a mesma forma: entrada que o contrato manda recusar com `422` na borda sai como `500` depois de tentar escrever — `id` repetido, `ordem` duplicada, elemento nulo na lista e `ordem` que alcança a faixa de trabalho do passo intermediário. O quinto é ausência de decisão sobre concorrência (SDR-002 não aplicada a esta rota: sem `@Version` e sem lock, dois `PUT` simultâneos perdem escrita em silêncio) e o sexto é falta de teto para o que uma escrita autenticada pode gravar. A task cumpriu a própria regra — recusa antes de qualquer escrita — para as duas recusas que conhece, e deixou de fora quatro que o corpo pode produzir |
| 2026-09-14 | `/implement TASK-02.2` — consulta e substituição do fluxo de etapas. A substituição é atômica e roda em **dois passos**, porque `etapa_projeto_ordem_unico` é único parcial e não é adiável: o passo intermediário desloca as ordens vigentes antes de reatribuí-las, exatamente como ACH-07 da revisão de 02.1 previu na migration. `fluxoConfigurado` passa a ser emitido em `GET /v1/projetos`, derivado por `EXISTS` na mesma consulta, e `AusenciaDeNMaisUmIT` segue verde. Critério 10 devolve à tela de criação a frase que ACH-07 de 01.7 retirou, e `fluxoConfigurado` deixa de ser opcional no tipo do cliente. 168 testes, 70/98 — nenhuma regressão. Descoberta a pendência 18 |
| 2026-09-14 | Os 14 achados da revisão de TASK-02.1 fechados. Destaques: TechSpec v1.11 decide a garantia **estreita** da raia (`tarefa.raia_id` existe; o que nenhuma tabela carrega é raia na série de tempo); repositórios trocam `JpaRepository` por `Repository`, tornando a ausência de remoção física propriedade da interface; migration nova em vez de editar a aplicada, para não quebrar o checksum do Flyway; `EsquemaDoFluxoIT` dá ao esquema a cobertura que `ddl-auto=validate` não alcança, com poder de falha provado. 163 testes, 64/99 — nenhuma regressão |
| 2026-09-14 | `/code-review TASK-02.1` — 14 achados, 4 bloqueantes. Todos na distância entre o que os arquivos afirmam sobre si e o que garantem |
| 2026-09-11 | `/implement TASK-02.1` — EPIC-02 aberto: esquema de `etapa` e `raia`, entidades e repositórios |
| 2026-09-11 | EPIC-01 fechado. Revisões de TASK-01.5 a 01.8 conduzidas e todos os achados corrigidos, inclusive os 6 bloqueantes de TASK-01.7 (redirecionador aberto, CVEs de produção, cabeçalhos de segurança) e os 2 de TASK-01.8 (escrita antes do `403`, transação única sem poder de falha) |
| 2026-09-10 | `/implement` de TASK-01.1 a 01.6 e as respectivas revisões; ambiente Docker, segurança, sessão autenticada, RBAC e tratamento de erro em pé |
| 2026-09-10 | `/tests` — suíte congelada v1.0; `/analyze --pre-implement` aprova o GATE-CONSISTENCIA na 9ª execução |
| 2026-09-09 | `/prd` → `/techspec` → `/analyze` → `/tasks`: PRD congelado no GATE-SPEC, TechSpec com 4 SDRs, 8 épicos e 43 tasks. Coleção `infra/docker` elaborada (ADR-011/012/013) |
| 2026-09-04 | `/intent` → `/classify` → `/context` → `/discovery` → `/shape` → `/solution` → `/design`: demanda `kanban-tarefas` percorre a camada de Controle e o primeiro diamante |
| 2026-09-03 | Sistema inicializado via `init.py`; skills e flows migrados para o contrato novo |
